@page "/hello"
@model Ecohabits.Pages.HelloModel
@{
    Layout = "_Layout";
}

<h1>Hello, world!</h1>

<ul>
    <li><strong>Domínio (Host):</strong> @Model.RequestHost</li>
    <li><strong>IP remoto:</strong> @Model.RemoteIpAddress</li>
    <li><strong>Timestamp (UTC):</strong> @Model.Timestamp</li>
</ul>

<p>Use <code>/hello</code> para verificar se o domínio está apontando corretamente.</p>